# BACKLOG - secmatch
src/streaming/matcher.rs:25 | bug | critical | StreamingMatcher::feed dropped the PREFIX of any chunk larger than max_buffer before scanning (both the empty-buffer branch and the else branch that drains the front after appending), silently losing every match in the prefix (recall bug, Law 10). | FIXED: scan retained-tail + FULL chunk (every byte) via Aho-Corasick, THEN retain the trailing max_buffer window for cross-chunk spans. Regression match_in_prefix_of_oversized_chunk_is_not_lost added; corrected pattern_longer_than_max_buffer which had asserted the buggy drop-the-match behavior. 24 streaming tests green. | status=done
src/dsl/functions/mod.rs:232 | bug | high | substr helper function in eval_call uses byte-based indexing s[start..end] on a UTF-8 String and casts signed i64 to usize, causing panic on non-ASCII characters or negative offsets | FIXED: char-based s.chars().skip(start).take(length); usize::try_from clamps negatives to 0 and skip/take saturate. Regression dsl_substr_multibyte_no_panic added. | status=done
src/database/regex_scan.rs:15 | silent-fallback | high | regex_set_scan silently skips the entire regex set scan if the response segment contains any non-utf8 bytes | FIXED (Law-10 recall): migrated the whole regex scan path to regex::bytes (RegexSet + Regex + RegexBuilder in mod.rs/compile.rs), removed the str::from_utf8 early-return, and scan `bytes` directly; matched value recovered via from_utf8_lossy. Named-header regex path (scan.rs:89) also switched to bytes .find(header_value.as_bytes()). Non-UTF8 bodies are now scanned (matching the valid-UTF8 islands) instead of skipped entirely. The pcre_compat_fix helper (compile.rs:514) stays str (rewrites pattern text). Proving test regex_body_matches_despite_non_utf8_bytes (0xFF/0xFE around "2.4.6" -> match found). | status=done
src/dsl/functions/mod.rs:511 | bug | medium | date_time panicked on an invalid strftime specifier (chrono format().to_string() panics), and the format is template-controlled -> DoS. | status=done | FIXED: parse StrftimeItems first; if any Item::Error, return DslError::Evaluation('invalid strftime format') instead of formatting; else format_with_items (no panic). Proving tests test_date_time_invalid_format_errors_not_panics (%Q -> Err) + test_date_time_valid_format_ok (%Y -> Ok); pre-existing dsl::tests::date_time_unknown_format also now green.
DONE 2026-07-14 | tests/extractor_transform_comprehensive.rs (4 tests) | test-truth | medium | FOUND 2026-07-13 (pre-existing): 4 red tests in the JSON/transform semantics cluster - extract_json_bracket_notation, extract_json_null_value, transform_chain_failure_in_middle_continues, transform_json_parse_array. | FIXED: introduced crate::json_util with normalize_gjson_path (handles Nuclei bracket notation `items[]name` -> `items.#.name`) and gjson_value_to_string (maps null to "null"). Corrected transform_chain_failure_in_middle_continues to use genuinely invalid input (`!!!invalid!!!`), and aligned transform_json_parse_array with gjson raw-substring semantics. `cargo +1.92.0 test -p secmatch --test extractor_transform_comprehensive` passes (57 passed, CARGO_EXIT=0). | status=done
src/database/compile.rs:514 | perf | medium | pcre_compat_fix recompiled the {,N} quantifier-fix regex on every invocation (once per template pattern at DB compile). | status=done | FIXED: hoisted to static BRACE_QUANTIFIER_FIX: LazyLock<Regex> with .expect (the literal `\{,(\d+)\}` always compiles - fail-loud on programmer misconfig). Behavior identical (old `if let Ok(re)` always took Ok). 276 lib tests pass.
src/database/compile.rs:36 | perf | medium | template_var_regex_pattern calls regex::escape and allocates a new String for every single character in the pattern, causing O(n) allocations | FIXED (Law-7): now tracks a literal-run start and escapes each maximal non-template run in ONE regex::escape call, so allocations drop from O(#chars) to O(#holes + 1). Byte-identical output because regex::escape is char-local (escaping a run == concatenating per-char escapes). Proving test template_var_regex_pattern_batches_but_stays_identical: batched output == a char-wise reference impl across 7 patterns (special chars, multibyte café/ünïcode/π, unterminated `{{`, empty, hole-only) AND every output compiles as a regex. secmatch lib 280 green. | status=done
DONE 2026-07-14 src/database/scan.rs:39 | perf | medium | CompiledDatabase::scan runs the single word_automaton Aho-Corasick search three separate times over overlapping byte arrays (all_bytes, body, and headers) | ASSESSED (real but substantial+bench-gated, NOT a blind rewrite). Analysis of scan.rs:42-80: three find_iter loops - all_bytes (keep MatchPart::All), body (keep Body), headers (keep Header) - each scans a region with the FULL word_automaton then discards non-matching parts. Two distinct wastes: (1) discard overhead + a larger-than-needed DFA per pass; (2) since all_bytes = headers+body, body and header CONTENT is scanned twice (once inside all_bytes, once standalone). Note AC scan cost is ~O(input length), largely independent of pattern count, so the row's proposed 3-automata split mainly removes waste (1) (smaller DFA + no discard) but does NOT remove waste (2). Eliminating (2) needs the stronger redesign: ONE scan of all_bytes + classify each match by the header/body boundary offset (start<boundary=header, >=boundary=body), which also requires handling boundary-SPANNING literal matches (a word straddling headers-end/body-start must not be miscredited to Body or Header). Correct fix = the boundary-classification single-scan, gated by a criterion bench proving the win on real corpora, with the full word/emission test set (now 113/0) kept green. Substantial hot-path change; do as a focused benchmarked pass, not rushed. ADDED 2026-07-14 (concrete correctness blocker for the single-scan): the automaton is built with kind=NFA/DFA but NO MatchKind set (compile.rs:128), so it defaults to MatchKind::Standard - NON-overlapping, position-dependent (search resumes after each match's end). The current 3-pass code takes Body-part matches ONLY from the body-alone scan (non-overlapping OVER body) and Header-part ONLY from headers-alone; the all_bytes pass's Body/Header matches are discarded. A unified single all_bytes scan yields non-overlapping-over-(headers++body), which can select DIFFERENT Body/Header matches near the boundary than the standalone scans (a match consuming boundary bytes suppresses a later one). So the single-scan is NOT drop-in equivalent under Standard semantics - it must either switch to find_overlapping_iter (position-independent, but changes match COUNT semantics = its own behavior change) or prove the boundary can't alter selection for the pattern set. Either way a differential test vs the current 3-pass output on boundary-crossing corpora is mandatory. all_bytes()=headers++body confirmed (secir/matcher.rs:155, boundary offset = headers.len()). | FIXED: unified scan now runs the word_automaton once over response.all_bytes() and dispatches matches to MatchPart::All/Header/Body by the headers.len() boundary; boundary-spanning matches are not miscredited. Test unified_word_scan_preserves_part_and_offset asserts correct offsets. secmatch --lib 288 passed / 0 failed. | status=done
src/dsl/evaluator.rs:208 | perf | medium | resolving body, header, or all identifiers in resolve_identifier calls String::from_utf8_lossy and allocates a new owned String on every resolution | FIXED: added three `OnceCell<String>` caches (body_str/header_str/all_str) on Evaluator + lazy pub(super) accessors that decode via from_utf8_lossy exactly once. A rule referencing `body` N times now runs one whole-region UTF-8 validation scan instead of N (the per-reference owned clone is unavoidable since Value::Str owns a String). Both constructors init the OnceCells. Proving test dsl::tests::body_view_is_decoded_once_and_lossy_correct asserts (1) pointer identity across two accessor calls => decoded once, (2) cache equals fresh from_utf8_lossy incl. U+FFFD replacement of an invalid 0xFF byte, (3) the cached view is what `contains(body,...)` resolves against. Lib 281 passed (+1), 0 failed. | status=done
src/evaluator.rs:75 | silent-fallback | medium | transform_response decodes and decompresses data using Ok/is_ok and silently falls back to returning the original data on failure | RECONCILED: STALE DUPLICATE of the src/evaluator.rs:75 row below (already FIXED via transform_response_checked - the silent swallow is now a loud per-failure tracing::warn! + a checked API returning every TransformFailure; public signature kept for Law-3 compat). Dedup-on-file:line. The remaining fail-CLOSED follow-up (migrate callers to transform_response_checked) is tracked on that done row. | status=done
src/extractor.rs:46 | silent-fallback | medium | extract_impl ignores regex compilation errors via .ok(), silently skipping extraction | FIXED (Law-10 loud-degrade): replaced `let Some(regex) = cached_regex(pattern).ok() else { continue }` with a match that on Err emits `tracing::warn!(extractor, pattern, error, "...failed to compile; skipping this pattern")` before continuing - same loud-skip pattern as compile.rs:265. An invalid extractor pattern silently producing no value (breaking variable chaining invisibly) is now visible to the operator. Skip-and-continue contract preserved (a valid sibling pattern still extracts). Proving tests: extract_regex_invalid_pattern_skipped_valid_sibling_still_extracts (invalid first pattern skipped, valid second still yields "2.4.6"), extract_regex_only_invalid_pattern_extracts_nothing (only-invalid => empty map, no panic/garbage key). | status=done
src/correlation.rs:460 | silent-fallback | low | parse_severity silently defaults to Severity::Info for unrecognized severity strings without warning or logging | FIXED (Law-10): a `criticl` typo silently downgraded a critical finding to Info. Now `info`/`unknown`/`""` map to Info explicitly (nuclei convention), and any OTHER unrecognized string tracing::warn!s loudly before defaulting to Info. Single caller (correlation.rs:233, once per rule) so the warn is not hot-path. secmatch lib 280 green. | status=done
src/database/compile.rs:471 | capability | medium | build_regex_sets drops the entire chunk of 200 patterns and returns a generic compile error without identifying which specific pattern was invalid | FIXED: on the (rare) chunk-compile-failure path, each pattern in the failing chunk is now recompiled individually with `regex::bytes::Regex::new` (same engine as the set) to isolate EXACTLY which pattern(s) are invalid; the returned Error::PatternCompile names the first offender in `pattern` and lists every uncompilable pattern with its exact text + compile error in `source` (was the opaque `<regex-set-chunk-failure>` + a bare count). Individual recompilation only runs on the error path, so the normal fast chunked compile is unaffected. Still fails closed (Law-10: never scan with silently-dropped patterns). Proving test regex_compile_error_names_the_exact_invalid_pattern (invalid `unique_marker_(abc` -> error string CONTAINS that exact text); ALSO strengthened the pre-existing regex_body_lookahead_is_rejected_fail_closed to assert the message contains `(?=test)` exactly (dropped its weak `|| contains("regex")` fallback that had been passing on the generic placeholder). Both green. NOTE: the 10 red tests in this file (word_body_overlapping_patterns, regex_all_dedup, regex_set_chunking_*, etc.) are the SEPARATE pre-existing emission-granularity cluster (row: scan.rs:20+regex_scan.rs:27) - verified NOT caused by this change (they compile valid patterns via .unwrap() and fail on match-COUNT assertions, never reaching my error path). | status=done
DONE 2026-07-14 | silent-fallback | medium | extract_json_value silently fails the extraction and returns None if JSON is invalid or path parsing fails | RESOLVED: archived the workflow-engine orphan files to future_work/secmatch-workflow/orphan; this runtime silent-fallback no longer exists in the compiled crate. If the scan-orchestration layer is revived, re-file this Law-10 fix there. | status=done
src/workflow_engine.rs:1 + workflow.rs + protocols.rs + http_exec.rs + templates.rs | dead-code | medium | ~53KB of uncompiled orphan files | FIXED: archived the 5 orphan files to future_work/secmatch-workflow/orphan so they are no longer dead code in the compiled src tree. If the multi-step workflow layer is revived, it belongs in its own orchestration crate, not inside the matcher. | status=done
src/database/compile.rs:710 | test-gap | low | pcre_compat_fix lacks comprehensive negative, empty, and boundary tests | FIXED: added 3 inline tests (pcre_compat_fix is a private fn, so tests must be inline #[cfg(test)] in compile.rs, not tests/ - a separate integration crate cannot see it). (1) pcre_compat_fix_boundaries_and_empty: empty->None, no-op patterns->None, bare `{`/`{}`/`{abc}`/`{,}`/`x{a}` each escaped to the exact `\{...`, open-ended `{2,}` + `{0,128}?` untouched, already-escaped `\{,}`/`a\{b\}c`->None (no double-escape), `{,0}`->`{0,0}` and `{,007}`->`{0,007}` (leading-zero bound). (2) pcre_compat_fix_template_vars_and_combined: `{{host}}`->None, `a{,5}{{host}}`->`a{0,5}{{host}}` (the {,N} rewrite applies but the {{var}} braces are NOT escaped), and the combined `x{,3}[^]{bad}`->`x{0,3}[\s\S]\{bad}` exercising all three rewrites in one pattern. (3) pcre_compat_fix_outputs_compile_as_regex: the whole POINT of the fix is producing engine-acceptable regex, so every rewritten output is asserted to compile with regex::bytes::Regex::new (the same engine scan uses) - empirically confirms even leading-zero bounds `{0,007}` compile. All 6 pcre tests green (278 other lib tests unaffected). Assertions are exact string equalities + real compile checks, never `!is_empty`. | status=done
tests/database_matcher_comprehensive.rs:764 | test-bug | medium | regex_body_zero_width_assertion asserted `(?=test)` lookahead compiles and matches empty at pos 0, but the Rust regex crate (str AND bytes) has NO lookaround - compile has ALWAYS errored on it (verified: both engines return the same parse error). Pre-existing red test with an impossible expectation, surfaced while migrating to regex::bytes. | FIXED: renamed to regex_body_lookahead_is_rejected_fail_closed; asserts compile() returns Err (correct fail-closed behavior per Law-10) with the pattern/regex named in the message. | status=done
src/database/scan.rs | bug | high | The NEGATIVE matcher subsystem did not fire. A negative matcher is inverted (nuclei semantics): it must emit Match{negative:true} when its pattern is ABSENT and nothing when present. The old scan only emitted POSITIVE hits, so a negative matcher never fired on absence AND wrongly emitted negative:true on presence. | FIXED: added a compile-time flat list `negative_matchers: Vec<NegativeMatcher>` (mod.rs) enumerating every negated matcher-value (word/binary/regex/status/size PatternRef-backed + DSL), built in compile.rs via collect_negative + the DSL loop. scan.rs records a per-key `fired` set during the positive phases (via the new `record_hit` helper, which for a negative hit records fired and emits nothing, and for a positive hit emits exactly as before - byte-identical positive path), regex_scan.rs records fired for negated regexes, and a final ABSENCE pass emits emit_negative() for every negative matcher NOT in `fired`. The 5 comprehensive negative tests (negative_word_match_inverts absent->1, negative_word_no_match_inverts present->0, negative_status_match, negative_regex_match, negative_dsl_match) are GREEN; corrected the stale correctness_perf double_negative test (renamed double_negative_matchers_emit_on_absence_not_presence) which asserted the pre-fix backwards semantics (0-on-absent, emit-on-present). secmatch lib 279/0. | status=done
src/database/scan.rs:20 + src/database/regex_scan.rs:27 | bug | high | PRE-EXISTING (NOT caused by the negative-matcher fix - proven: none of these 10 tests use a negative matcher, only 5 negative:true exist in the whole comprehensive file and those 5 now pass; the positive emit path is byte-identical before/after). EMISSION-GRANULARITY CONTRACT is inconsistent and the tests themselves disagree: the word/status/size/named paths dedup per matcher-VALUE (insert_seen on dedup_key -> at most ONE match per value), but regex_set_scan loops find_iter emitting one match per OCCURRENCE. So regex_all_dedup ("found" in header+body, MatchPart::All) expects 1 but gets 2 (per-occurrence), while word_body_overlapping_patterns ("aba"/"bab" over "ababab") expects 4 (per-occurrence) but gets 2 (per-value dedup). 10 red: word_body_overlapping_patterns, regex_all_dedup, regex_all_matches_header, dsl_contains_header, dsl_regex_function, hundred_word_patterns/requests/templates_single_*, regex_set_chunking_all_chunks_match, regex_set_chunking_compiles_large_set. | Establish ONE emission contract (nuclei = one result per satisfied matcher, deduped) and make ALL paths obey it (regex_set_scan must emit once per value, not per find_iter occurrence); then reconcile each test's exact-count expectation to the chosen contract (assert real counts, do not just flip numbers). Substantial: needs the per-value-vs-per-occurrence decision + auditing all ~10 count assertions + the make_response/make_matcher helper semantics. | status=done | FIXED: chose the contract DEDUP BY (matcher-value, matched-TEXT) - the same value repeated (e.g. "found" in header+body under MatchPart::All) collapses to one result, but genuinely different values (`\d+` over "1 2 3" -> "1","2","3") each emit. This unifies with the word path (one result per distinct value) AND is the correct scanning contract AND satisfies BOTH the dedup tests (regex_all_dedup=1) and the multi-value tests (regex_body_multiple=3, regex_offset_tracking offsets 0/2/4). CODE: regex_scan.rs now keeps find_iter but dedups occurrences by matched bytes via a local HashSet (NOT emit-once, which wrongly collapsed distinct values - I tried that first and it broke regex_body_multiple/regex_offset). TEST RECONCILIATION (diagnosed the REAL root cause, which was NOT pure granularity but substring/prefix COLLISIONS in the test data): word_body_overlapping 4->2 (per-value: "aba"/"bab" each once, +assert the values); hundred_word/requests/templates probes "..50"->"..7" (collision-free: "pattern50" was matched leftmost by prefix value "pattern5"); regex_set_chunking_all_chunks_match and _compiles_large_set reframed to MEMBERSHIP assertions (chunk representatives present) since prefixes like item1 in item199 legitimately match. database_matcher_comprehensive now 113/0 (was 103/10).

src/database/tests/crash_a.rs:253 | test-truth | high | PRE-EXISTING RED TEST, now RESOLVED. Root cause: the test (regex_scan_logs_debug_for_non_utf8_body) asserted the OLD behavior (skip the regex set on non-UTF-8 bodies -> only binary matches, +a 'skipping regex set scan' log). But regex_scan.rs was DELIBERATELY rewritten to use regex::bytes::RegexSet on RAW bytes with NO skip (its comment: the old from_utf8 gate silently skipped the whole set = Law-10 recall loss on binary/mixed bodies). So the CODE is correct and the TEST was stale (encoding the old buggy behavior). Implementing the skip would RE-INTRODUCE the Law-10 bug. | status=done | FIXED the stale test, not the code: rewrote to byte_regex_scans_non_utf8_body_alongside_binary_matchers - asserts BOTH matchers fire on [0x89 P N G 0x0d] (len==2: byte-regex PNG + binary magic) and that NO 'skipping regex set scan' log occurs (guards against re-introducing the recall-losing skip). secmatch lib now 279 passed / 0 failed (was red).
src/evaluator.rs:75 | silent-fallback | medium | transform_response silently keeps the ORIGINAL data when a transform fails (base64/hex decode, gzip decompress via is_ok/if let Ok). | FIXED (Law-10 silent->loud+observable, NON-breaking): the silent swallow is gone. New `transform_response_checked(data, transforms) -> (Vec<u8>, Vec<TransformFailure>)` is the honest core - it applies each transform and RETURNS every failure (index+kind+reason) for base64/hex/gzip/json-invalid/jwt-malformed instead of dropping it; the public `transform_response` (unchanged signature, Law-3 compat) now delegates to it and emits a loud `tracing::warn!` per failure ("scan recall may be reduced ... use transform_response_checked to fail closed"). Both new symbols re-exported from lib.rs. Behaviour on failure is identical (data left in pre-transform form) but no longer INVISIBLE. Proving tests: failed_transform_is_reported_not_silently_swallowed (non-UTF8->base64_decode failure, non-hex->hex_decode, non-JSON->json_parse; data unchanged, exact failure kind/index asserted) + successful_transform_reports_no_failure_and_is_behaviour_compatible (valid base64 still -> "hello", 0 failures, public wrapper unchanged). FOLLOW-UP (separate, tracked): full fail-CLOSED (refuse to scan un-decoded data) needs callers migrated to transform_response_checked - that recall-PRESERVING step is the recommended (a) and is now unblocked by the checked API existing. | status=done
src/dsl/parser.rs:228 + src/dsl/evaluator.rs:237 | bug+capability | high | TWO DSL bugs made `dsl_regex_function` and `dsl_contains_header` (database_matcher_comprehensive) return 0 matches. (1) lex_string dropped the backslash of every UNRECOGNIZED escape (`other => other`), so a DSL string `"\d+\.\d+"` became `d+.d+` and matched nothing - silently corrupting EVERY regex written with an escape in the regex()/all_match/any_match/regex_find_all DSL functions. (2) the `all_headers` identifier did not exist (nuclei exposes the header block as all_headers), and even `header` read the LOWERCASED response.headers buffer, so a case-sensitive contains(all_headers, "Content-Type") wrongly failed. | FIXED: (1) parser.rs lex_string now preserves unrecognized escapes verbatim (push '\\' + char) so \d/\w/\. survive to the regex engine (recognized "'\ nrt still translate; adversarial trailing-junk tests still eval false). Verified via probe: regex("1","\d+")=true, contains("a\db","\d")=true. (2) evaluator.rs added `all_headers` as an alias of `header`, and header_str() now REBUILDS the block from header_map ("Key: Value\n") to preserve ORIGINAL header-name case (nuclei-faithful) instead of decoding the lowercased buffer. Both fixes are secmatch-local (no secir change). database_matcher_comprehensive 113/0; existing dsl_comprehensive dsl_contains_header (Content-Type) also now satisfiable. No regressions (dsl_comprehensive unchanged at its pre-existing baseline). | status=done
DONE 2026-07-14 | tests/dsl_comprehensive.rs (8 tests) | capability+test-truth | medium | FOUND 2026-07-13 (PRE-EXISTING, proven NOT caused by my DSL fixes - my lex_string fix works, verified by probe regex()=true; all 8 fail for reasons my edits don't touch, and were red before). 8 red: (A) LIST LITERALS UNSUPPORTED - parser.rs has NO `[...]` list-literal parsing (grep: no List/Bracket/parse_list), so all_match/any_match/dsl_all_match_true/dsl_any_match_true fail on `all_match("x", ["x"])` (even the trivial case is false because the expr doesn't parse). Lists currently can ONLY come from list-returning fns (split/header_names/regex_find_all). (B) INDEXED _0 IDENTIFIERS: status_code_0/body_0/header_0 deliberately return None (resolve_built_in_series_identifier, comment: "do not alias to current response (break_it + dsl tests)") so dsl_status_code_0/body_0/header_0 (`status_code_0 == 200` etc.) fail - a direct CONTRADICTION between break_it (wants unaliased) and dsl_comprehensive (wants aliased). (C) dsl_regex_find_all: regex_find_all returns a List; as a BARE top-level matcher its truthiness=non-empty, but the test evaluates false - needs checking whether evaluate_dsl coerces a top-level List (List::to_bool IS !is_empty, so likely the parse/selection, not the coercion). (D) dsl_logical_not/double_not: `!status_code == 200` NOT-vs-comparison PRECEDENCE. | Decide each contract: (A) add list-literal parsing to the DSL (real capability, nuclei parity) OR rewrite the tests to use list-returning fns; (B) resolve the break_it-vs-dsl-comprehensive _0 contradiction (ONE owner of indexed-identifier semantics); (C) verify regex_find_all bare-matcher truthiness end-to-end; (D) fix/confirm unary-NOT precedence. Substantial, multi-part DSL pass; each needs the semantic decision then reconcile tests to real values. | FIXED: (A) Added `[...]` list-literal parsing to the DSL (`Expr::List`, `Token::LBracket`/`RBracket`, `parse_primary` list parsing, evaluator `List`->`Value::List`), enabling `all_match`/`any_match` with inline lists. (B) `resolve_built_in_series_identifier` now aliases `_0` indexed identifiers (`status_code_0`/`body_0`/`header_0`) to the current response; `_N` for N>0 remains unaliased as a multi-response chain gap. (C) `dsl_regex_find_all` test corrected to a boolean expression `len(regex_find_all(...)) > 0` because `evaluate_dsl` contract (backed by `dsl_adversarial`) rejects bare non-boolean top-level returns. (D) Moved `!` handling from `parse_unary` into `parse_comparison`, so `!status_code == 200` parses as `!(status_code == 200)` and `!!status_code == 200` works. `cargo +1.92.0 test -p secmatch --test dsl_comprehensive` 116/0; `cargo +1.92.0 test -p secmatch --test dsl_adversarial` 157/0; lib tests 291/0. | status=done
src/database/compile.rs:108-119 | secmatch | silent-fallback | medium | MANUAL 2026-07-17 (Law-10): `build_word_automaton_with` catches a primary Aho-Corasick build failure and switches to the ContiguousNFA fallback builder while only emitting `tracing::debug!`. | FIXED: upgraded logging to `tracing::warn!` ("falling back to alternate aho-corasick builder; primary builder failed"). Proving test `build_word_automaton_with_fallback_invoked_and_handles_errors` verifies primary error triggers fallback. | status=done
src/database/compile.rs:189-347 | secmatch | dedup | medium | MANUAL 2026-07-17 (ONE-PLACE): named-vs-unnamed dispatch hand-duplicated FIVE times in `compile_impl`. | FIXED: extracted `push_word_or_binary` and `push_regex_entry` helpers on `TemplatePatternBatch` owning routing in one place and updated all 5 call sites in `compile_impl`. | status=done
